name: docexpress
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.0.0
  flutter_riverpod: ^2.5.1  # Changed to 2.5.1 for compatibility with StateNotifier
  riverpod_annotation: ^2.3.5  # Added for better code generation support
  google_fonts: ^6.3.2
  flutter_secure_storage: ^9.2.4
  equatable: ^2.0.7
  hive_flutter: ^1.1.0
  dio: ^5.7.0
  file_picker: ^8.1.2
  intl: ^0.19.0
  flutter_animate: ^4.5.0
  flutter_slidable: ^3.1.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13  # Added for code generation
  riverpod_generator: ^2.4.3  # Added for better Riverpod experience

flutter:
  uses-material-design: true